// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Manages an organization-level Logfire delivery schedule. A channel assignment with a `scheduleId` notifies its channel only inside the schedule's windows. Use it in `channelAssignments` on `logfire.Alert` and in `alerts.<tier>.channel_assignments` on `logfire.Slo`. Deleting a schedule makes the channel assignments that use it notify at all times. The provider credential needs `organization:read_channel` and `organization:write_channel`. The schedules API needs Logfire v2026-09-23.01 or newer.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as logfire from "@pydantic/pulumi-logfire";
 *
 * // Weekday office hours in London. Use the schedule in a channel assignment on
 * // `logfire_alert` or `logfire_slo` to notify a channel only inside its windows.
 * const officeHours = new logfire.Schedule("officeHours", {
 *     label: "Office hours",
 *     timezone: "Europe/London",
 *     windows: [{
 *         days: [
 *             1,
 *             2,
 *             3,
 *             4,
 *             5,
 *         ],
 *         end_time: "18:00",
 *         start_time: "09:00",
 *     }],
 * });
 * // Several windows: weekday evenings and all of the weekend.
 * const outOfHours = new logfire.Schedule("outOfHours", {
 *     label: "Out of hours",
 *     timezone: "America/New_York",
 *     windows: [
 *         {
 *             days: [
 *                 1,
 *                 2,
 *                 3,
 *                 4,
 *                 5,
 *             ],
 *             end_time: "23:59",
 *             start_time: "18:00",
 *         },
 *         {
 *             days: [
 *                 6,
 *                 7,
 *             ],
 *             end_time: "23:59",
 *             start_time: "00:00",
 *         },
 *     ],
 * });
 * ```
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * Import an existing schedule by its UUID. The API has no schedule list and no
 *
 * lookup by label. The UUID is the `schedule_id` of any channel assignment that
 *
 * uses the schedule, for example in `logfire_slo.alerts` or in the alert
 *
 * API response.
 *
 * ```sh
 * $ pulumi import logfire:index/schedule:Schedule office_hours' "9f9b2f9e-aaaa-bbbb-cccc-ddddeeeeffff"
 * ```
 */
export class Schedule extends pulumi.CustomResource {
    /**
     * Get an existing Schedule resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ScheduleState, opts?: pulumi.CustomResourceOptions): Schedule {
        return new Schedule(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'logfire:index/schedule:Schedule';

    /**
     * Returns true if the given object is an instance of Schedule.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Schedule {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Schedule.__pulumiType;
    }

    /**
     * Schedule name.
     */
    declare public readonly label: pulumi.Output<string>;
    /**
     * IANA time zone of the windows, for example `Europe/London`.
     */
    declare public readonly timezone: pulumi.Output<string>;
    /**
     * Time windows in which the schedule delivers. At least one.
     */
    declare public readonly windows: pulumi.Output<outputs.ScheduleWindow[]>;

    /**
     * Create a Schedule resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ScheduleArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ScheduleArgs | ScheduleState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ScheduleState | undefined;
            resourceInputs["label"] = state?.label;
            resourceInputs["timezone"] = state?.timezone;
            resourceInputs["windows"] = state?.windows;
        } else {
            const args = argsOrState as ScheduleArgs | undefined;
            if (args?.label === undefined && !opts.urn) {
                throw new Error("Missing required property 'label'");
            }
            if (args?.timezone === undefined && !opts.urn) {
                throw new Error("Missing required property 'timezone'");
            }
            if (args?.windows === undefined && !opts.urn) {
                throw new Error("Missing required property 'windows'");
            }
            resourceInputs["label"] = args?.label;
            resourceInputs["timezone"] = args?.timezone;
            resourceInputs["windows"] = args?.windows;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Schedule.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Schedule resources.
 */
export interface ScheduleState {
    /**
     * Schedule name.
     */
    label?: pulumi.Input<string>;
    /**
     * IANA time zone of the windows, for example `Europe/London`.
     */
    timezone?: pulumi.Input<string>;
    /**
     * Time windows in which the schedule delivers. At least one.
     */
    windows?: pulumi.Input<pulumi.Input<inputs.ScheduleWindow>[]>;
}

/**
 * The set of arguments for constructing a Schedule resource.
 */
export interface ScheduleArgs {
    /**
     * Schedule name.
     */
    label: pulumi.Input<string>;
    /**
     * IANA time zone of the windows, for example `Europe/London`.
     */
    timezone: pulumi.Input<string>;
    /**
     * Time windows in which the schedule delivers. At least one.
     */
    windows: pulumi.Input<pulumi.Input<inputs.ScheduleWindow>[]>;
}
