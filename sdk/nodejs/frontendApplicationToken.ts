// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Experimental: the backing management API is currently enabled only in Logfire staging environments. Manages one active token for a Logfire frontend application. Rotate in two applies with two resource blocks: add the replacement and deploy its `token`, then remove the old resource to revoke it. The provider never revokes an old token while creating a new one. Token issuance is not retried automatically because the operation is not idempotent; after an uncertain failure, list the application's tokens and adopt the one unambiguous new active token. Before destroying the whole application tree, set the last token's `revokeOnDestroy` to `false` in a separate apply; application deletion then revokes every attached token.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as logfire from "@pydantic/pulumi-logfire";
 *
 * const current = new logfire.FrontendApplicationToken("current", {
 *     projectId: logfire_frontend_application.browser.project_id,
 *     applicationId: logfire_frontend_application.browser.id,
 *     adoptTokenId: logfire_frontend_application.browser.token_id,
 * });
 * // First apply with both resources. Deploy the replacement output everywhere.
 * // Then remove `current` and apply again to revoke the previous token.
 * const replacement = new logfire.FrontendApplicationToken("replacement", {
 *     projectId: logfire_frontend_application.browser.project_id,
 *     applicationId: logfire_frontend_application.browser.id,
 *     revokeOnDestroy: true,
 * });
 * export const rotatedBrowserWriteToken = replacement.token;
 * ```
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * ```sh
 * $ pulumi import logfire:index/frontendApplicationToken:FrontendApplicationToken current "project-id/application-id/token-id"
 * ```
 */
export class FrontendApplicationToken extends pulumi.CustomResource {
    /**
     * Get an existing FrontendApplicationToken resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: FrontendApplicationTokenState, opts?: pulumi.CustomResourceOptions): FrontendApplicationToken {
        return new FrontendApplicationToken(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'logfire:index/frontendApplicationToken:FrontendApplicationToken';

    /**
     * Returns true if the given object is an instance of FrontendApplicationToken.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is FrontendApplicationToken {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === FrontendApplicationToken.__pulumiType;
    }

    /**
     * Existing active token to adopt instead of issuing another. Use the application's `tokenId` for the initial handoff, then remove this resource only after a separately managed replacement has been deployed.
     */
    declare public readonly adoptTokenId: pulumi.Output<string | undefined>;
    /**
     * UUID of the frontend application.
     */
    declare public readonly applicationId: pulumi.Output<string>;
    /**
     * Timestamp when the token was created.
     */
    declare public /*out*/ readonly createdAt: pulumi.Output<string>;
    /**
     * Timestamp when the token was last used, when known.
     */
    declare public /*out*/ readonly lastUsedAt: pulumi.Output<string>;
    /**
     * UUID of the project that owns the application.
     */
    declare public readonly projectId: pulumi.Output<string>;
    /**
     * Revoke this token when its resource is destroyed. Before destroying the entire application tree, set this to `false` in a separate apply for the last token so application deletion can revoke all attached tokens.
     */
    declare public readonly revokeOnDestroy: pulumi.Output<boolean>;
    /**
     * Token status. A managed token remains in state only while active.
     */
    declare public /*out*/ readonly status: pulumi.Output<string>;
    /**
     * Active restricted browser token. The provider recovers this value on refresh and import.
     */
    declare public /*out*/ readonly token: pulumi.Output<string>;

    /**
     * Create a FrontendApplicationToken resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: FrontendApplicationTokenArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: FrontendApplicationTokenArgs | FrontendApplicationTokenState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as FrontendApplicationTokenState | undefined;
            resourceInputs["adoptTokenId"] = state?.adoptTokenId;
            resourceInputs["applicationId"] = state?.applicationId;
            resourceInputs["createdAt"] = state?.createdAt;
            resourceInputs["lastUsedAt"] = state?.lastUsedAt;
            resourceInputs["projectId"] = state?.projectId;
            resourceInputs["revokeOnDestroy"] = state?.revokeOnDestroy;
            resourceInputs["status"] = state?.status;
            resourceInputs["token"] = state?.token;
        } else {
            const args = argsOrState as FrontendApplicationTokenArgs | undefined;
            if (args?.applicationId === undefined && !opts.urn) {
                throw new Error("Missing required property 'applicationId'");
            }
            if (args?.projectId === undefined && !opts.urn) {
                throw new Error("Missing required property 'projectId'");
            }
            resourceInputs["adoptTokenId"] = args?.adoptTokenId;
            resourceInputs["applicationId"] = args?.applicationId;
            resourceInputs["projectId"] = args?.projectId;
            resourceInputs["revokeOnDestroy"] = args?.revokeOnDestroy;
            resourceInputs["createdAt"] = undefined /*out*/;
            resourceInputs["lastUsedAt"] = undefined /*out*/;
            resourceInputs["status"] = undefined /*out*/;
            resourceInputs["token"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["token"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(FrontendApplicationToken.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering FrontendApplicationToken resources.
 */
export interface FrontendApplicationTokenState {
    /**
     * Existing active token to adopt instead of issuing another. Use the application's `tokenId` for the initial handoff, then remove this resource only after a separately managed replacement has been deployed.
     */
    adoptTokenId?: pulumi.Input<string>;
    /**
     * UUID of the frontend application.
     */
    applicationId?: pulumi.Input<string>;
    /**
     * Timestamp when the token was created.
     */
    createdAt?: pulumi.Input<string>;
    /**
     * Timestamp when the token was last used, when known.
     */
    lastUsedAt?: pulumi.Input<string>;
    /**
     * UUID of the project that owns the application.
     */
    projectId?: pulumi.Input<string>;
    /**
     * Revoke this token when its resource is destroyed. Before destroying the entire application tree, set this to `false` in a separate apply for the last token so application deletion can revoke all attached tokens.
     */
    revokeOnDestroy?: pulumi.Input<boolean>;
    /**
     * Token status. A managed token remains in state only while active.
     */
    status?: pulumi.Input<string>;
    /**
     * Active restricted browser token. The provider recovers this value on refresh and import.
     */
    token?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a FrontendApplicationToken resource.
 */
export interface FrontendApplicationTokenArgs {
    /**
     * Existing active token to adopt instead of issuing another. Use the application's `tokenId` for the initial handoff, then remove this resource only after a separately managed replacement has been deployed.
     */
    adoptTokenId?: pulumi.Input<string>;
    /**
     * UUID of the frontend application.
     */
    applicationId: pulumi.Input<string>;
    /**
     * UUID of the project that owns the application.
     */
    projectId: pulumi.Input<string>;
    /**
     * Revoke this token when its resource is destroyed. Before destroying the entire application tree, set this to `false` in a separate apply for the last token so application deletion can revoke all attached tokens.
     */
    revokeOnDestroy?: pulumi.Input<boolean>;
}
